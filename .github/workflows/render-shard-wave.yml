name: Render one wave of shards

# One wave of a sharded render: a matrix of at most 256 shard jobs.
#
# This is a reusable workflow rather than a job in render-world.yml because a matrix
# expands to at most 256 entries and Actions cannot generate a variable number of jobs.
# A world needing more shards than that is rendered in sequential waves, and the caller
# declares one job per wave slot, each calling this file with that wave's shard ids.
#
# Every shard here is independently resumable. Its map directory - tiles and rstate
# together - is cached under a key carrying the plan's fingerprint, and a shard that
# renders to completion writes a marker beside its output. A re-dispatched run restores
# the cache, checks the marker, and skips every shard that is already done. A run that
# dies in wave 7 therefore costs wave 7 and not the six waves before it.
#
# See design/packages/render-actions/src/resume/ for the logic, which is under test, and
# docs/resumable-renders.md for why the cache holds rstate while the merge refuses to.

on:
  workflow_call:
    inputs:
      wave:
        description: Which wave this is, for job names and the run summary
        type: number
        required: true
      shards:
        description: JSON array of shard ids this wave renders
        type: string
        required: true
      map-id:
        description: >-
          BlueMap's own sanitized storage id (render-world.yml's `plan` job output, from
          design/packages/render-actions/src/bluemap.ts's sanitizeMapId) - already run
          through the same rule BlueMap itself applies to a map's config-file name before
          using it as a directory name, NOT the raw --map-id a user typed. Every path in
          this file that looks for rendered output depends on that already being correct;
          see issue #47.
        type: string
        required: true
      map-name:
        type: string
        required: true
      accept-download:
        description: '1 to allow the Minecraft client download, 0 to refuse it'
        type: string
        required: true

permissions:
  contents: read

env:
  # This project's own pinned commit of https://github.com/Ding-Ding-Projects/worldlens,
  # cloned fresh by the render job below rather than assumed to sit beside
  # `.github/workflows` in whichever repository dispatched render-world.yml, which calls
  # this reusable workflow. A repository this application bootstraps for somebody else
  # carries only the three workflow files it writes and a marker - no `design/` of its own
  # - so every step below that used to read the planner out of that checkout now reads it
  # out of a `toolchain/` clone pinned to this commit instead. See the render job's own
  # "Check out the render toolchain" step.
  #
  # A `workflow_call` reusable workflow does not inherit the caller's `env:` block, so this
  # is duplicated from render-world.yml rather than shared - keep the two values equal, and
  # advance both in the same commit the same way render-world.yml's copy is advanced.
  WORLDLENS_TOOLCHAIN_COMMIT: 9f52322cdfe284553499486bb1a8ffd2916331e3

jobs:
  render:
    name: Wave ${{ inputs.wave }} shard ${{ matrix.shard }}
    runs-on: ubuntu-24.04
    # Below GitHub's own six hour ceiling. The render step has a shorter timeout still,
    # so a shard that runs out of time fails that one step rather than having the job
    # cancelled underneath it - which is what lets the cache be saved and the partial
    # output uploaded instead of thrown away.
    timeout-minutes: 350
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(inputs.shards) }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      # This project's own tooling, fresh at a pinned commit, rather than assumed to sit
      # beside `.github/workflows` in whichever repository dispatched render-world.yml. A
      # repository this application bootstraps for somebody else carries only the three
      # workflow files it writes and a marker - it has no `design/` of its own - so every
      # step below that used to read that directory out of the checkout above now reads it
      # out of this clone instead. See WORLDLENS_TOOLCHAIN_COMMIT near the top of this file
      # for how the pin is advanced.
      - name: Check out the render toolchain
        shell: bash
        run: |
          set -euo pipefail
          git clone --quiet https://github.com/Ding-Ding-Projects/worldlens.git toolchain
          git -C toolchain checkout --quiet "$WORLDLENS_TOOLCHAIN_COMMIT"

      - uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4
        with:
          version: 10

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: toolchain/design/pnpm-lock.yaml

      - uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4
        with:
          distribution: temurin
          # Must match the version `build-jars.yml` compiles the jar with. It compiles at
          # 25, and running that jar on 21 fails with `UnsupportedClassVersionError: class
          # file version 69.0` before a single tile is drawn - which is exactly what
          # happened, and which surfaced three jobs later as a missing artifact rather
          # than as the version mismatch it was.
          java-version: '25'

      - name: Install
        working-directory: toolchain/design
        run: pnpm install --frozen-lockfile

      - name: Build the planner
        working-directory: toolchain/design
        run: pnpm --filter "@worldlens/render-actions..." run build

      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: bluemap-cli
          path: cli

      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: world
          path: world-in

      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: render-plan

      # Where this shard sits: which merge group takes it on, what its artifact is
      # called, and the cache key it saves under. The key carries the plan fingerprint,
      # so a cache saved for a different shard layout can never be restored on top of
      # this one - that rstate would claim work this shard never did.
      - name: Locate this shard
        id: shard
        run: |
          node toolchain/design/packages/render-actions/dist/cli.js waves \
            --plan shard-plan.json \
            --shard '${{ matrix.shard }}' \
            --run-id '${{ github.run_id }}' \
            --run-attempt '${{ github.run_attempt }}' \
            --github-output "$GITHUB_OUTPUT"

      # Restore rather than the full cache action, so the save below is an explicit step
      # that still runs after a render that ran out of time.
      - name: Restore this shard's render state
        id: restore
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            bluemap-out
            bluemap-data
          key: webapp-v2-${{ steps.shard.outputs.cache-key }}
          restore-keys: webapp-v2-${{ steps.shard.outputs.cache-restore-prefix }}

      # Only output carrying a completion marker that agrees with what is on disk counts
      # as finished. Anything else is unfinished and is rendered again, continuing from
      # whatever the restore brought back.
      - name: Is this shard already finished?
        id: resume
        run: |
          node toolchain/design/packages/render-actions/dist/cli.js resume-check \
            --plan shard-plan.json \
            --shard '${{ matrix.shard }}' \
            --storage-root "$GITHUB_WORKSPACE/bluemap-out/maps" \
            --map-id '${{ inputs.map-id }}' \
            --github-output "$GITHUB_OUTPUT" \
            --summary "$GITHUB_STEP_SUMMARY"

      # Every path written here is absolute. The BlueMap CLI resolves its data directory
      # and its storage root against the process working directory rather than against
      # the config folder, so a relative path quietly puts the map somewhere else.
      - name: Write this shard's BlueMap config
        if: steps.resume.outputs.complete != 'true'
        run: |
          node toolchain/design/packages/render-actions/dist/cli.js config \
            --plan shard-plan.json \
            --shard '${{ matrix.shard }}' \
            --world "$GITHUB_WORKSPACE/world-in" \
            --config-dir "$GITHUB_WORKSPACE/bluemap-config" \
            --data-dir "$GITHUB_WORKSPACE/bluemap-data" \
            --storage-root "$GITHUB_WORKSPACE/bluemap-out/maps" \
            --web-root "$GITHUB_WORKSPACE/bluemap-out" \
            --map-name '${{ inputs.map-name }}' \
            --threads 4 \
            --accept-download '${{ inputs.accept-download }}'

      # A standard runner has 16 GB. 10 GB of heap leaves enough for the OS and the
      # runner agent; the render is disk- and CPU-bound long before it is heap-bound.
      #
      # `continue-on-error` with a step timeout, deliberately. A shard that runs out of
      # time has produced hours of real tiles, and the steps after this one are what get
      # them off the runner before it disappears. Failing the job here would skip them.
      - name: Render
        id: render
        if: steps.resume.outputs.complete != 'true'
        continue-on-error: true
        timeout-minutes: 300
        run: java -Xmx10G -jar "$GITHUB_WORKSPACE/cli/bluemap-cli.jar" -c "$GITHUB_WORKSPACE/bluemap-config" -r -g

      # Written only after a clean exit. Its absence is the signal that this shard was
      # cut off, and a resume must never mistake a truncated write for finished work.
      - name: Mark this shard finished
        if: steps.resume.outputs.complete != 'true' && steps.render.outcome == 'success'
        run: |
          node toolchain/design/packages/render-actions/dist/cli.js shard-complete \
            --plan shard-plan.json \
            --shard '${{ matrix.shard }}' \
            --storage-root "$GITHUB_WORKSPACE/bluemap-out/maps" \
            --map-id '${{ inputs.map-id }}' \
            --run-id '${{ github.run_id }}' \
            --run-attempt '${{ github.run_attempt }}'

      # Saved whether the render finished or not: an unfinished shard's tiles and rstate
      # are exactly what makes the next dispatch cheap. Skipped on an exact key hit,
      # because nothing new was rendered.
      - name: Save this shard's render state
        if: always() && steps.restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            bluemap-out
            bluemap-data
          key: webapp-v2-${{ steps.shard.outputs.cache-key }}

      - name: Report what this shard produced
        id: report
        if: always()
        shell: bash
        run: |
          set -uo pipefail
          map="bluemap-out/maps/${{ inputs.map-id }}"
          # Not `set -e`: this step runs after a render that may have produced nothing at
          # all, and a `find` over a directory that does not exist must report zero rather
          # than take the step down with it.
          hires=0
          if [ -d "$map/tiles/0" ]; then
            hires=$(find "$map/tiles/0" -type f | wc -l)
          fi
          {
            echo "### Wave ${{ inputs.wave }}, shard ${{ matrix.shard }}"
            echo ""
            echo "- hires tiles: $hires"
            echo "- map size: $(du -sh "$map" 2>/dev/null | cut -f1 || echo unknown)"
            echo "- render step: ${{ steps.render.outcome || 'skipped, already finished' }}"
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "${{ steps.render.outcome }}" = "failure" ]; then
            echo "::warning::Shard ${{ matrix.shard }} did not finish. Its state is cached; re-dispatch this workflow to carry it on."
          fi
          echo "hires=$hires" >> "$GITHUB_OUTPUT"

      # A tiny per-shard receipt is the only reliable way for the final verifier to
      # distinguish a completed marker from an artifact that merely existed. The
      # fragment carries the exact run/attempt, wave and shard provenance; the final
      # receipt job combines these fragments without inventing cross-run evidence.
      - name: Write shard receipt fragment
        if: always()
        shell: bash
        env:
          RENDER_OUTCOME: ${{ steps.render.outcome || 'skipped' }}
          RESUME_COMPLETE: ${{ steps.resume.outputs.complete || 'false' }}
          HIRES: ${{ steps.report.outputs.hires || '0' }}
        run: |
          set -euo pipefail
          mkdir -p receipt-fragments
          available=$(df --output=avail -B1 . | tail -n1 | tr -d '[:space:]')
          filesystem=$(df --output=source . | tail -n1 | xargs)
          node -e 'const [outcome,resume,hires,available,filesystem,wave,shard,run,attempt]=process.argv.slice(1); const fs=require("fs"); const identity=JSON.parse(fs.readFileSync("receipt-fragments/plan-identity.json","utf8")); const now=new Date().toISOString(); fs.writeFileSync(`receipt-fragments/wave-${wave}-shard-${shard}.json`, JSON.stringify({phase:"render-merge-peak",...identity,wave:Number(wave),shard:Number(shard),artifactName:`wave-${wave}-shard-${shard}.json`,outcome,alreadyFinished:resume==="true",hiresTiles:Number(hires),recordedAt:now,freeBytes:Number(available),availableBytes:Number(available),filesystem}))' "$RENDER_OUTCOME" "$RESUME_COMPLETE" "$HIRES" "$available" "$filesystem" '${{ inputs.wave }}' '${{ matrix.shard }}' '${{ github.run_id }}' '${{ github.run_attempt }}'

      - name: Upload shard receipt fragment
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: render-receipt-wave-${{ inputs.wave }}-shard-${{ matrix.shard }}
          path: receipt-fragments
          if-no-files-found: warn
          retention-days: 30

      # `continue-on-error` above is right for the case it was written for - a shard that
      # ran out of time has hours of real tiles, and the steps after it are what get them
      # off the runner. It is wrong for a render that failed having produced nothing: that
      # job reported success, uploaded no artifact because there were no files to upload,
      # and the run broke three jobs later with "Artifact not found for name: webapp",
      # which points at the merge and says nothing about the engine refusing to start.
      #
      # That is exactly what a Java version mismatch did. So: a failed render that drew at
      # least one tile is still a partial success worth collecting, and a failed render
      # that drew nothing fails here, next to the reason.
      - name: A render that produced nothing is a failed render
        if: always() && steps.render.outcome == 'failure' && steps.report.outputs.hires == '0'
        shell: bash
        run: |
          set -euo pipefail
          # One annotation, then the explanation as ordinary log lines. This was written as
          # three quoted strings on one `echo`, separated by runs of spaces, which `echo`
          # collapses into single spaces - so the message read as one run-on sentence and
          # this is what tripped shellcheck's SC2291. A GitHub annotation is a single line anyway.
          echo "::error::Shard ${{ matrix.shard }} rendered no tiles at all, so the engine did not run."
          echo "Check the Render step's output above for the real reason."
          echo "Its exit was swallowed deliberately, so that a shard which merely ran out of time"
          echo "could still hand over the tiles it had already drawn - but a shard that drew"
          echo "nothing has no tiles to hand over, and is a failure rather than a partial success."
          exit 1

      # Uploaded whether or not it finished. The marker inside says which it was, and the
      # merge refuses any shard whose marker is missing rather than merging it short.
      # `include-hidden-files` is off, so nothing here is named with a leading dot.
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: ${{ steps.shard.outputs.shard-artifact }}
          path: bluemap-out/maps
          retention-days: 1
          compression-level: 1

      # The webapp shell is identical from every shard, so exactly one of them uploads it.
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: inputs.wave == 1 && matrix.shard == 0
        with:
          name: webapp
          path: |
            bluemap-out
            !bluemap-out/maps
          retention-days: 1
